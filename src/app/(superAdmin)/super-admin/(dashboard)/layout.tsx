import React from 'react'

function layout({ children }: { children: React.ReactNode }) {
    return (
        <div>
            dashboard layout
            {children}
            dashboard layout
        </div>
    )
}

export default layout
